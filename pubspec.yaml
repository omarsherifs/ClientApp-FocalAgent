name: client_app_focal_agent
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  bloc: ^8.1.2
  dartz: ^0.10.1
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3

  flutter_localizations:
    sdk: flutter
  get_it: ^7.6.4
  intl: ^0.18.0
  lottie: ^2.7.0
  dio: ^5.4.0
  http_mock_adapter: ^0.6.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/animation/
  generate: true
